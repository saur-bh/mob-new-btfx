# Login Completion Flow
# This flow handles the final steps after PIN creation

appId: com.bitfinex.bfxdev
name: "Login Completion"
description: "Complete login process by handling special fee screen and verifying portfolio"
---
# Handle special fee discount screen if it appears
- runFlow:
    when:
      visible: "Special fee discounts.*"
    commands:
      - tapOn:   # Close button

# Take a screenshot to see what's available
- takeScreenshot: "after_login.png"

# Check for various possible main screen indicators
- assertVisible:
    text: "Portfolio"
    optional: true
- assertVisible:
    text: "Wallet"
    optional: true
- assertVisible:
    text: "Trading"
    optional: true
- assertVisible:
    text: "Home"
    optional: true
